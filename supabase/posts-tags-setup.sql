-- ============================================================
-- AIVIEW 블로그 글 태그(주제 키워드·해시태그) 컬럼
-- Supabase 대시보드 → SQL Editor 에 붙여넣고 1회 실행하세요.
-- 용도: 블로그 글에 SEO·검색 노출용 키워드/해시태그를 저장.
--       발행 페이지의 메타데이터(keywords)·본문 태그 칩으로 서버 렌더됩니다.
-- 기존 RLS(anon select / 관리자 write)는 posts 정책을 그대로 따릅니다.
-- ============================================================

alter table public.posts add column if not exists tags text[];
