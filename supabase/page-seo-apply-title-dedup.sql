-- ============================================================
-- 도입문의(/apply) 제목 중복 제거
-- Supabase 대시보드 → SQL Editor 에 붙여넣고 1회 실행하세요.
--
-- 전역 타이틀 템플릿("%s · AI면접")이 자동으로 붙기 때문에,
-- DB 제목 중간의 "AI면접"을 빼서 최종 <title>에 AI면접이 한 번만 나오게 한다.
--   변경 전: 도입 문의 — AI면접 무료로 시작하기  → "...· AI면접" (AI면접 2회)
--   변경 후: 도입 문의 — 무료로 시작하기        → "...· AI면접" (AI면접 1회)
-- published(적용) 상태는 유지.
-- ============================================================

update public.page_seo set
  title = '도입 문의 — 무료로 시작하기',
  updated_at = now()
where path = '/apply';
